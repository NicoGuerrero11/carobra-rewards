# Used by Astro middleware and the server-side /api/v1 proxy in development and
# production. Browser requests remain same-origin and never call FastAPI directly.
SITE_BACKEND_BASE_URL=http://127.0.0.1:3001
REWARDS_V2_PREVIEW_ENABLED=false
REWARDS_V2_PREVIEW_USERNAME=
REWARDS_V2_PREVIEW_PASSWORD=
REWARDS_V2_TEST_ACCESS_KEY=
