## 1. Notification actions

- [x] 1.1 Add the upper-right bulk read action with loading/disabled states, bounded persisted reads, truthful error feedback and synchronized counters for bulk and individual actions.

## 2. Verification

- [x] 2.1 Add isolated persistence fixtures and browser tests for reload, single/bulk actions, failures/retry, empty states, keyboard and mobile; build and visually inspect the result.

## 3. User-requested visual follow-up

- [x] 3.1 Replace oversized legacy notification presentation with compact, readable Carobra styling matching Activity, retaining all content/actions and explicit read-state labels.
- [x] 3.2 Verify typography, contrast, responsive layout and all reading regressions; visually inspect desktop/mobile and build.
