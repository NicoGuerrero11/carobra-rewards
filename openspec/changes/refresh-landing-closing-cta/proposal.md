## Why

The last landing block retains an oversized title, long operational copy and two competing buttons. The user approved a compact blue closing invitation with one white registration button and a secondary sign-in text link.

## What Changes

- Replace the closing block with the approved heading and brief, level-qualified invitation.
- Emphasize `Únete a Rewards` as a white button and show `¿Ya tienes cuenta? Inicia sesión` beneath it.
- Keep the existing section anchor, registration/login routes, footer and neighboring content unchanged.
- Provide responsive sizing, keyboard focus and a static no-JavaScript experience.

## Capabilities

### New Capabilities
- `landing-closing-cta`: Compact public closing invitation with clear registration/sign-in hierarchy.

### Modified Capabilities
None. Existing discovery destinations and authentication behavior remain unchanged.

## Impact

Public landing component, retired closing-only CSS, landing tests and OpenSpec evidence. No API, database, business-rule, dependency, authentication or footer changes.
